alpha 0.3
Добавлено:
офллайн идл
престиж
после каждых 25 апгрейдов х2 на первом и втором апгрейде

Изменено:
Элементы появляются при их достижении
Изменены переменные апгрейдов и их математическая прибавка.

Баги:
при заходе на страницу после оффлайна отображается не верно данные, берутся с HTML (первоначальные значения)

alpha 0.31
После недостаток денег красный цвет держится дольше
Исправление багов

Баги:
при заходе на страницу после оффлайна и при сбросе отображается не верно данные, берутся с HTML (первоначальные значения)
При чите если нажать отмена возвращается null и ломает игру(мб как наказание использовать)

alpha 0.33
Значение теперь отображаются верно.
Апгрейды больше не изчезают при покупке. Так же при покупе 3 апгрейда и скипа покупки 2 апгрейда он не исчезает.

alpha 0.34
Баг с читом исправлен.

alpha 0.34a
Изменено:
-Стоимость апргейдов не такая анальная: +1/5 от стоимости за уровень вместо x*2+стоимость
-Закомментил алерт про выпитое в офлайне - мешает. 320 строка
-Реформат html под grid
-Типизированы переменные типа firstNumber -> levelOf1Upgrade / upgrade1Cost -> costOf1Upgrade
-Отдельная функция увеличения за каждые 25 уровней упразднена и добавлена в основную с условием одной строчкой
-Переменная income теперь относится только к общему инкому в секунду, инком от каждой абилки считается
 переменной incomeFromXUpgradeTotal += incomeFromXUpgradePerLevel

Визуал:
-Шрифт Exo 2 из гугла
-Фоновое изображение
-Эффекты головы

Баги(ИСПРАВЛЕНО):
-Анимация вылетающих стаканов при инкаме равном 0
-Удвоение инкама при первом апргрейде - остаток от 0/25 такой же как и от 25/25

alpha 0.35
Изменено:
-число апгрейдов увеличено до 10
-выведены на хтмл и прописаны в сохранениях\сбросах\престижах
-убраны лишние переменные в коде
-двойной апгрейд на lvl % 25 (по сути каждый 26)
-сделал сохранение incomeFromXUpgradeTotal и его сброса(можно использовать для престижей\апгрейдов)

alpha 0.36
-Добавлен опыт и уровни с очками опыта
-Выведены на страницу стата про опыт чтобы просто было
-сброс прогресса из памяти заменен на одну строку

Баги
-большое число текущих литров не сохраняется (начинается от ***+е2*) (решение возможно конвертации всего или необходимую часть из number в BigInt)
-при наезде стаканчиков на лицо оно становится не кликабельным
-чек анимации не выключает анимацию лица

alpha 0.36a
-апгрейды спрятаны по фен шую
-баг с большими числами исправлен. Все необходимые числа из number переведены в BigInt.
